/** @type {import('next').NextConfig} */
const nextConfig = {
  // add external image configuration here
  images: {
    domains: ["avatars.githubusercontent.com"],
  },
};

export default nextConfig;
